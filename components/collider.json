{
	name : COLLIDER,
	nonSerialized : {
		mask : { type : uint16_t, default : 0xffff },
		category : { type : uint16_t, default : 0x0001 },
		ur : { type : ivec2 },
		bl : { type : ivec2 },
		collisions : { type : int16_t, count : 4 }
	}
}
